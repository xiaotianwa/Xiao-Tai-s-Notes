# 日记本 UI 接口与字段预留记录

本文件记录 `日记本.png` 复刻后暂未落到数据模型的内容，后续补接口或字段时按这里接入。

## 当前已接入

- 日记列表、新建、编辑、详情、删除确认弹窗已重构为同一套浅紫日记本视觉。
- 新建/编辑仍保留本地保存、图片选择、媒体备份映射、收藏、编辑和删除能力。
- 日记列表只展示本地数据库 / 同步数据；无记录时展示空状态，不再展示设计稿静态占位卡片。
- 编辑页和详情页已隐藏底部 Tab，列表页保留底部 Tab。
- 位置、多标签、草稿状态已接入 `AppEntry` 结构化字段。

## 字段预留

- `detailImageCount`: 详情缩略图的 `+2` 为设计占位，后续应由图片数量计算。

## 建议模型扩展

```json
{
  "locationText": "地点文本",
  "location": { "lat": 0, "lng": 0 },
  "tags": ["标签"],
  "draft": false,
  "updatedAt": "2026-06-02T18:00:00+08:00"
}
```
